# Optional in local dev: leave unset to use Vite's proxy (same origin → /api → localhost:5001).
# Set both when building for production or when the API is on another host.
# VITE_API_URL=http://localhost:5001
# VITE_BASE_URL=http://localhost:5001
